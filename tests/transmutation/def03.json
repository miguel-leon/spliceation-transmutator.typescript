{
  "$schema": "../../transmutation-definition/schema.json",
  "definition": [
    {
      "class": "outer",
      "match": "'.*'",
      "recursion": [
        {
          "class": "inner",
          "match": "\\d+",
          "recursion": true
        }
      ]
    }
  ]
}
